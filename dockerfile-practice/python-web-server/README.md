Create a Dockerfile for a Python web server using Flask
Here are the details:

Your app has a file called app.py which starts a basic Flask web server

The server listens on port 5000

You need to install Flask using pip

When the container runs, it should automatically start the Flask app


